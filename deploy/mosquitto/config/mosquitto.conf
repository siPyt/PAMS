# Minimal Mosquitto config for the PAMS field gateway.
# Anonymous access is enabled for the local demo/lab network only.
# For production, add authentication (password_file) and TLS.

listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
log_type error
log_type warning
log_type notice
log_type information
